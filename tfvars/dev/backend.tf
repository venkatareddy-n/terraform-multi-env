# here backend means backend block we uses this block for S3 and DynamoDB configuration

bucket         = "venkatswan-dev"
key            = "venkatswan-dev-key" # every repo should have S3-key
region         = "us-east-1"
dynamodb_table = "venkat-db-dev"